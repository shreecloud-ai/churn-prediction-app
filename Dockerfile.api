FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .

# Increase timeout + retries + use PyPI mirror if needed
RUN pip install --no-cache-dir \
    --default-timeout=100 \
    -r requirements.txt

COPY . .

# For API service
EXPOSE 8000
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]